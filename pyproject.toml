[project]
name = "cloudcleaner"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "langgraph",
    "langchain-groq",
    "langchain-core",
    "boto3",
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "pydantic",
]